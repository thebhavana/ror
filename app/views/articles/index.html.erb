<h1>Articles</h1>
<ul>
    <% @articles.each do |article| %>
    <li><a href ="/articles/<%= article.id %>">
        <%= article.title %>
        </a>
    </li>
    <li>
        <%= link_to article.body, article%>
    </li>
    <% end %>
</ul>

  <a href ="/articles/new"><h2>Create new Article </h2>
<%= link_to "New Article", new_article_path %>
